-----
version: "3"

services:
  filebrowser:
    image: hurlenko/filebrowser
    user: "${UID}:${GID}"
    ports:
      - 443:8080
    volumes:
      - /media/USBHDD/share/:/data
      - /media/USBHDD/share/docker/filebrowser/configs:/config
    environment:
      - FB_BASEURL=/filebrowser
    restart: always
